import React from 'react';

const WhatsAppSender: React.FC = () => (
  <div className="p-4">
    <h2 className="text-lg font-bold">WhatsAppSender (temporarily disabled)</h2>
    <p className="text-sm text-slate-500">The WhatsApp sender UI is temporarily disabled to fix build errors. Restore the original UI when ready.</p>
  </div>
);

export default WhatsAppSender;
